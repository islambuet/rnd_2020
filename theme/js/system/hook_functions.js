/**
 * Created by Shaiful Islam on 15/04/20.
 */
function system_pre_tasks(params={})
{
    system_resized_image_files=[];
    if(params.controller!==undefined)
    {
        // controller condition code
    }
}